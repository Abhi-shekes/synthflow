# Dev-only config for the optional `mqtt` docker-compose profile (see
# app.services.stream_producers / MQTTOutput) — anonymous, unencrypted
# access on the default MQTT port. Never use this configuration for
# anything reachable outside a local docker-compose network.
listener 1883
allow_anonymous true
